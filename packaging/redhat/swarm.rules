%ifarch sparc
CONSERVATIVE_OPTIMIZATION=yes %configure
%endif

%ifarch ppc
CFLAGS="-fdollars-in-identifiers -O2 -g" %configure --with-ffidir=/usr --disable-jar
%endif

%ifarch i386
%configure
%endif

%{gcc_path} make JAVAC=/usr/bin/jikes

%install
rm -rf $RPM_BUILD_ROOT
mkdir $RPM_BUILD_ROOT

cd %SWARM_SRC_DIR

